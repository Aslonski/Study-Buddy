language: ruby
rvm:
- 2.0.0-p647

bundler_args: --jobs=1
script:
 - bundle exec rake db:create
 - bundle exec rake db:migrate
 - bundle exec rake db:seed
 - bundle exec rake spec
cache: bundler
services:
 - postgresql
